header
    if user
        <img id="profile-img" onclick="openUserDetails();" src="#{user.picture}" style="margin-left:10px; height:40px; width:40px; border-radius:50%">
        <a id="login-out" href="/logout">Logout</a>
    else
        <a id="login-out" href="/login">Login</a>